      interface
        subroutine serialize ( buffer, serialize_id, buffer_size ) bind(c)
          use iso_fortran_env
          use iso_c_binding
          implicit none
          integer(c_int64_t), intent(in)          :: serialize_id
          integer(c_int32_t), intent(in)          :: buffer_size
          integer*1, allocatable, intent(inout), dimension(:) :: buffer
        end subroutine serialize
      end interface
